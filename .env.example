# PostgreSQL database connection string (replace with your own in .env)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Application port (Railway uses 8080 in production)
PORT=8080

# Public base URL of your hosted backend
BASE_URL=https:https://tinylink-production-d45a.up.railway.app/
